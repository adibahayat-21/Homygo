<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error | HomyGo</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Orbitron:wght@400;700&display=swap" rel="stylesheet">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/new.css">
  <style>
    body {
      margin: 0;
      font-family: 'Orbitron', sans-serif;
      /* background: url("/sean-oulashin-KMn4VEeEPR8-unsplash.jpg") no-repeat center center/cover; */
      background: rgb(222, 5, 45);
      position: relative;
      min-height: 100vh;
    }

    /* Overlay */
    body::before {
      content: "";
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      /* background-color: rgba(242, 237, 237, 0.8); */
      background-color: rgb(222, 5, 45);
      z-index: -1;
    }

    .error-container {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 80vh;
      text-align: center;
    }

    .error-alert {
      /* background: linear-gradient(to right, #ffa4d6, #b7e3f1); */
      color: white;
      padding: 30px 50px;
      border-radius: 20px;
      box-shadow: 0 6px 18px rgba(0,0,0,0.2);
      font-size: 28px;
      font-weight: 600;
      margin-bottom: 25px;
       font-family: 'Bebas Neue', sans-serif; /* big attention grabbing */
      letter-spacing: 2px;
  text-transform: uppercase;
    }

    .error-btn {
      /* background: linear-gradient(to right, #65e2cf, #ffaccf); */
      background-color: white;
      padding: 12px 30px;
      color: rgb(222, 5, 45);
      border-radius: 30px;
      font-size: 18px;
      font-weight: 600;
      text-decoration: none;
      transition: 0.3s;
    }
    h1 {
  font-family: 'Orbitron', sans-serif;
  text-shadow: 2px 2px 8px rgba(0,0,0,0.3);
}

    .error-btn:hover {
      transform: scale(1.05);
      /* background: linear-gradient(to right, #ffa4c7, #7eebd3); */
      /* color: white; */
      color: white;
      background-color: rgb(222, 5, 45);
    }

    @media (max-width: 600px) {
      .error-alert {
        font-size: 22px;
        padding: 20px 30px;
      }

      .error-btn {
        font-size: 16px;
        padding: 10px 25px;
      }
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-alert">
      <%= message %>
    </div>
    <h1 style="color: white;">We are Sorry, But there is an ERROR!</h1>
    <br>
    <a href="/" class="error-btn">Back to Home</a>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<!-- <p><%=err.stack%></p>    this is for when we want to know all the details about the error from where the error is generated -->